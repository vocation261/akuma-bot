services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: prod-botkumax
    container_name: prod-botkumax
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data-prod:/app/data
      - ./.env:/app/.env:ro